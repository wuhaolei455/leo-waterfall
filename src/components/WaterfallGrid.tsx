import { WaterfallGridProps } from "../types";

function WaterfallGrid(props: WaterfallGridProps) {
    const { imageCount = 50, columnWidth = 400, gap = 16 } = props;

    return (
        <div>
            haha
        </div>
    )
}

export default WaterfallGrid;